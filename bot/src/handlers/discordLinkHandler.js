export default async function (interaction) {
	/**
	 * discord check linked users interval
	 * check if the user is linked by getting all linked users ids from database
	 * then get all users who have 
	*/
}