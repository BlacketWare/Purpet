import { Helmet } from "react-helmet";
// import styles from "./styles.module.css";

export default function Auctions() {
    return (
        <>
            <Helmet>
                <title>Auctions | Purpet</title>
                <meta name="description" content="Auction off your blooks to other users" />
            </Helmet>

        </>
    );
}